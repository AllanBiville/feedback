{% extends 'admin.html.twig' %}

{% block title %}Modifier catégorie{% endblock %}

{% block body %}

<link rel="stylesheet" href="{{ asset('css/crud.css') }}">
    <div class="container">
  <div class="card"></div>
  <div class="card">
    <h1 class="title">Modifier catégorie <br/>({{types_category.shortname}})</h1>
    {{ include('crud_categories/_form.html.twig', {'button_label': 'Modifier'}) }}
  </div>
</div>
{% endblock %}

