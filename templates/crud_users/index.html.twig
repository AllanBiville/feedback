{% extends 'admin.html.twig' %}

{% block title %}Classes
{% endblock %}

{% block body %}
	<link rel="stylesheet" href="{{ asset('css/crud.css') }}">
<div class="container-fluid">
	<h1 class="text-center mb-4">Affichage des classes</h1>
		<a  class="btn btn-success" href="{{ path('app_crud_users_new') }}">Créer classe</a>
		<br/><br/>
	<div class="table-containeur mx-auto">
		<table class="table table-responsive-xl table table-borderless table-hover">
			<thead>
				<tr class="tr-index">
					<th scope="col">ID</th>
					<th scope="col">Classes</th>
					<th scope="col">Actions</th>

				</tr>
			</thead>
			<tbody>
				{% for types_user in types_users %}
					<tr>
						<td>{{ types_user.id }}</td>
						<td>{{ types_user.name }}</td>
						<td>
							<a class="btn btn-warning" href="{{ path('app_crud_users_edit', {'id': types_user.id}) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i><br/>Modifier</a>
						<br/>
						{{ include('crud_users/_delete_form.html.twig') }}
                        </td>
					</tr>
				{% else %}
					<tr>
						<td colspan="5">Aucune données disponibles !</td>
					</tr>
				{% endfor %}


			</tbody>
		</table>
	</div>
	</div>
{% endblock %}
