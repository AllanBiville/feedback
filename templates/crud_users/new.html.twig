{% extends 'admin.html.twig' %}

{% block title %}Créer classe{% endblock %}

{% block body %}
   <link rel="stylesheet" href="{{ asset('css/crud.css') }}">
    <div class="container">
  <div class="card"></div>
  <div class="card">
    <h1 class="title">Créer classe</h1>
    {{ include('crud_users/_form.html.twig', {'button_label': 'Créer'}) }}
  </div>
</div>
{% endblock %}